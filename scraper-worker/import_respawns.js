import fs from 'fs'; import { supabase } from './src/db.js'; async function run() { const data = JSON.parse(fs.readFileSync('D:/projeto mirror/Discord/data/respawns.json', 'utf8')); console.log(Encontrados  respawns no JSON.); for(let r of data) { await supabase.from('respawns').upsert({ id: r.id, name: r.name, category: r.category }); } console.log('? Respawns importados com sucesso para o Supabase!'); } run().catch(console.error);
