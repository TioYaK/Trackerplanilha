const puppeteer = require('rebrowser-puppeteer');
const fs = require('fs');

async function checkIFrames() {
    // A quick script to connect to the running browser if possible, or we just inject this into processAutoInvites.js
}
